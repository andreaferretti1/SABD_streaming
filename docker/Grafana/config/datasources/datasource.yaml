apiVersion: 1

datasources:
  - name: InfluxDB-Progetto
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    jsonData:
      version: Flux
      organization: results
      defaultBucket: home
    secureJsonData:
      token: ${INFLUX_TOKEN}